# Supabase (self-hosted)
# IMPORTANT: Leave VITE_SUPABASE_URL empty or commented out to automatically use the current hostname.
# This allows the app to work from any IP address (localhost, LAN IP, WAN IP).
# The frontend will auto-detect and use: http://<current-hostname>:8000
#
# Only set VITE_SUPABASE_URL if you need to override the default behavior:
# VITE_SUPABASE_URL=http://192.168.1.10:8000
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
